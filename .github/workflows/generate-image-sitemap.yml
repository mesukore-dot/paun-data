name: Generate Image Sitemap

on:
  push:
    paths:
      - 'videos.json'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Generate Sitemap XML
        run: |
          python -c "
          import json

          # 1. 実際のファイルを読み込む
          with open('videos.json', 'r', encoding='utf-8') as f:
              data = json.load(f)

          site_url = 'https://wpmesucolle.wpx.jp/'

          # 2. 女優ごとにデータをまとめる
          actresses = {}
          for item in data:
              actressId = item.get('actressId')
              if not actressId:
                  continue
              
              actressId = str(actressId)
              
              # 4人の例外を自動変換
              mapping = {
                  'toumine_hinako': 'higashimine-hinako',
                  'watanabe_hono': 'watanabe-hono',
                  'satsuki_nao': 'satsuki_nao-2',
                  'hakata_iroha': 'hakata-iroha'
              }
              
              slug = mapping.get(actressId, actressId)
              
              gif_url = item.get('gif2Url') or item.get('gif1Url')
              if not gif_url:
                  continue
                  
              title = item.get('title', '')
              title = title.replace('&', '&amp;').replace('<', '&lt;').replace('>', '&gt;').replace('\"', '&quot;').replace('\'', '&apos;')
              
              if slug not in actresses:
                  actresses[slug] = []
              actresses[slug].append({'src': gif_url, 'title': title})

          # 3. テキスト処理で完璧なXMLを組み立てる（バックスラッシュのバグを完全消去！）
          xml_lines = []
          xml_lines.append('<?xml version=\"1.0\" encoding=\"UTF-8\"?>')
          xml_lines.append('<urlset xmlns=\"http://www.sitemaps.org/schemas/sitemap/0.9\" xmlns:image=\"http://www.google.com/schemas/sitemap-image/1.1\">')

          for slug, images in actresses.items():
              xml_lines.append('  <url>')
              xml_lines.append(f'    <loc>{site_url.rstrip(\"/\")}/{slug.strip(\"/\")}/</loc>')
              for img in images:
                  xml_lines.append('    <image:image>')
                  xml_lines.append(f'      <image:loc>{img[\"src\"]}</image:loc>')
                  if img['title']:
                      xml_lines.append(f'      <image:title>{img[\"title\"]}</image:title>')
                  xml_lines.append('    </image:image>')
              xml_lines.append('  </url>')

          xml_lines.append('</urlset>')

          # バックスラッシュを綺麗に消し去って、純粋なXMLとして保存する
          final_xml = '\n'.join(xml_lines).replace('\\\"', '\"')
          with open('sitemap-images.xml', 'w', encoding='utf-8') as f:
              f.write(final_xml)
          "

      - name: Commit and Push Changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add sitemap-images.xml
          git diff --quiet && git diff --staged --quiet || (git commit -m '🤖 画像サイトマップを全自動更新' && git push origin HEAD:main)
